properties:
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  refunds:
    description: |
      Container for refunds.
    items:
      $ref: ./GETRefundTypewithSuccess.yaml
    type: array
  success:
    description: Returns `true` if the request was processed successfully.
    type: boolean
type: object
