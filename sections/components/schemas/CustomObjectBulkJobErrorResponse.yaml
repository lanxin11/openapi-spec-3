properties:
  code:
    description: The error code.
    example: 71012520
    format: Int32
    type: integer
  message:
    description: The error message.
    example: input string "123" is not a valid UUID
    type: string
  record:
    $ref: ./CustomObjectRecordWithAllFields.yaml
  row:
    description: >-
      The CSV record row number. The custom object record data starts at the
      second row because the first row is the CSV header.
    example: 12
    format: Int32
    type: integer
type: object
