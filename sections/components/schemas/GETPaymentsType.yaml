properties:
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  payments:
    description: |
      Information about one or more payments:
    items:
      $ref: ./GETPaymentType.yaml
    type: array
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
