properties:
  city:
    maxLength: 40
    type: string
  country:
    description: >-
      Country; must be a valid country name or abbreviation. If using Zuora Tax,
      you must specify a country to calculate tax.
    maxLength: 64
    type: string
  county:
    maxLength: 32
    type: string
  postalCode:
    maxLength: 20
    type: string
  state:
    maxLength: 40
    type: string
  taxRegion:
    maxLength: 32
    type: string
type: object
