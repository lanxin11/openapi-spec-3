properties:
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  revenueEventDetails:
    description: >
      Represents a change to a revenue schedule, such as posting an invoice or
      distributing revenue.
    items:
      $ref: ./GETRevenueEventDetailWithoutSuccessType.yaml
    type: array
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
