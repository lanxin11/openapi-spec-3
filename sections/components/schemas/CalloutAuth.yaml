description: If `requiredAuth` is `true`, this object is required.
properties:
  domain:
    description: The domain of the callout auth.
    type: string
  password:
    description: The field is required when requiredAuth is `true`.
    type: string
  preemptive:
    description: >-
      Set this field to `true` if you want to enable the preemptive
      authentication.
    type: boolean
  username:
    description: The field is required when requiredAuth is `true`.
    type: string
type: object
