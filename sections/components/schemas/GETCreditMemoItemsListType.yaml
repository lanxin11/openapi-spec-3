properties:
  items:
    description: |
      Container for credit memo items.
    items:
      $ref: ./GETCreditMemoItemTypewithSuccess.yaml
    type: array
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  success:
    description: Returns `true` if the request was processed successfully.
    type: boolean
type: object
