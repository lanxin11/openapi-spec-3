allOf:
  - properties:
      id:
        description: >
          The sytem generated Id for the Order Line Item(OLI). Use this field to
          specify which OLI to update.
        format: UUID
        type: string
    required:
      - id
    type: object
  - $ref: ./OrderLineItemCommon.yaml
