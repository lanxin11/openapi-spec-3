properties:
  itemParts:
    description: |
      Container for payment part items.
    items:
      $ref: ./GETPaymentItemPartTypewithSuccess.yaml
    type: array
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  success:
    description: Returns `true` if the request was processed successfully.
    type: boolean
type: object
