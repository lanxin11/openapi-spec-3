allOf:
  - properties:
      memos:
        description: |
          The container for a list of debit memos.
        items:
          $ref: ./GETDebitMemoType.yaml
        maxItems: 50
        type: array
      success:
        description: Returns `true` if the request was processed successfully.
        type: boolean
    type: object
