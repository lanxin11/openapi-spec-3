allOf:
  - properties:
      invoices:
        description: |
          The container for a list of posted invoices.
        items:
          $ref: ./InvoicePostResponseType.yaml
        title: invoices
        type: array
      success:
        description: >
          Returns `true` if the request has one of invoices was posted
          successfully.
        type: boolean
    type: object
