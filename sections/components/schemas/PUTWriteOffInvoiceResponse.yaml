properties:
  creditMemo:
    description: >
      Container for the credit memo that is automatically generated when writing
      off invoices.
    properties:
      id:
        description: >
          The ID of the credit memo that is created when the invoice is written
          off.
        type: string
    type: object
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
