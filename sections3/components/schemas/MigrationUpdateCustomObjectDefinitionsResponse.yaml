additionalProperties: false
properties:
  actions:
    description: >-
      The actions of updating custom object definitions, to be performed as
      parts of the migration.  Currently only one action per migration is
      supported.
    items:
      $ref: ./CustomObjectDefinitionUpdateActionResponse.yaml
    maxItems: 1
    minItems: 1
    type: array
type: object
