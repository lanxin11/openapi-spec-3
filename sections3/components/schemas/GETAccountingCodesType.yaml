properties:
  accountingCodes:
    description: >
      An array of all the accounting codes in your chart of accounts. Each
      accounting code has the following fields.
    items:
      $ref: ./GETAccountingCodeItemWithoutSuccessType.yaml
    type: array
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
