description: ''
properties:
  sequenceSets:
    description: >
      Array of sequence sets configured for billing documents, payments, and
      refunds.
    items:
      $ref: ./GETSequenceSetResponse.yaml
    type: array
  success:
    description: |
      Indicates whether the call succeeded.
    type: boolean
type: object
