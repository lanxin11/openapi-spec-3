example: ''
properties:
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  revenueSchedules:
    description: |
      How revenue will be recognized over time.
    items:
      $ref: ./GETRSDetailForProductChargeType.yaml
    type: array
  success:
    description: Returns `true` if the request was processed successfully.
    type: boolean
type: object
