properties:
  startDate:
    description: >
      The date on which revenue automation starts. This is the first day of an
      accounting period.
    format: date
    type: string
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
  updatedBy:
    description: |
      The user who made the change.
    type: string
  updatedOn:
    description: |
      The date when the revenue automation start date was set.
    format: date-time
    type: string
type: object
