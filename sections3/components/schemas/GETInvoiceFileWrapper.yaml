properties:
  invoices:
    description: >
      Contains information about one or more invoices. The array contains a
      maximum of 300 invoices.
    items:
      $ref: ./GETInvoiceType.yaml
    type: array
  nextPage:
    description: >
      URL to retrieve the next page of the response if it exists; otherwise
      absent.
    format: URL
    type: string
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
