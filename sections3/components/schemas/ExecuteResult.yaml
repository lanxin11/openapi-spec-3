properties:
  Errors:
    description: ''
    items:
      $ref: ./ActionsErrorResponse.yaml
    type: array
  Id:
    description: >
      The ID of the object in the call. The value is the same as the value you
      provide in the `ids` field for the request.
    type: string
  Success:
    description: |
      Returns `true` if the call was processed successfully, otherwise `false`.
    type: boolean
type: object
