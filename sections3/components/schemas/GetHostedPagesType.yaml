properties:
  hostedpages:
    description: |
      Container for the hosted page information.
    items:
      $ref: ./GetHostedPageType.yaml
    type: array
  success:
    description: |
      Returns `true` if the request was processed successfully.
    type: boolean
type: object
